[project]
name = "pydeep"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.5.1",
    "langchain-openai>=1.1.12",
    "python-dotenv>=1.2.2",
]
