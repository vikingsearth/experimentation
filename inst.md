# Steps

## Making the project

1. cd to right location, mkdir your new project folder, cd into it
2. Run `uv init` along with `uv add langchain deepagents langchain-ollama langchain-antrhopic langchain-openai langgraph python-dotenv`
3. 